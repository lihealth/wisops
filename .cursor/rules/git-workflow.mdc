---
description: 新项目 Git 版本控制工作流（WisOps 团队规范）
alwaysApply: true
---

# Git 版本控制工作流

## 初始化新项目（一次性）

```powershell
# 1. 创建 .gitignore（必须先做）
# Python 项目必须排除：.venv/ __pycache__/ *.pyc
# 前端项目必须排除：node_modules/ dist/
# 敏感配置必须排除：.env .env.local

# 2. 初始化仓库
git init
git add .
git commit -m "init: 项目初始提交"

# 3. 关联 GitHub 并推送
git remote add origin https://github.com/lihealth/<仓库名>.git
git branch -M main
git push -u origin main
```

## Git 用户配置（环境初始化时确认）

```powershell
git config --global user.name "lihealth"
git config --global user.email "lihealth@163.com"
```

## 日常开发工作流

```powershell
# 开始新功能：从 main 新建分支
git checkout main
git pull
git checkout -b feat/<功能名>   # 如 feat/graph-ui

# 开发中：阶段性提交
git add .
git commit -m "feat: 描述本次变更"

# 推送分支
git push -u origin feat/<功能名>

# 功能完成后合并回 main
git checkout main
git merge feat/<功能名>
git push
```

## 分支命名规范

| 前缀 | 用途 | 示例 |
|------|------|------|
| `feat/` | 新功能 | `feat/graph-ui` |
| `fix/` | Bug 修复 | `fix/cors-error` |
| `docs/` | 文档更新 | `docs/readme` |
| `chore/` | 配置/工程 | `chore/add-gitignore` |

## Commit Message 规范

```
feat: 新增图谱录入页面
fix: 修复 graph-api CORS 配置
docs: 更新 README 启动说明
chore: 添加 .gitignore 排除 .venv
```

## .gitignore 必排除项

- `**/.venv/`、`**/__pycache__/`、`**/*.pyc`（Python）
- `**/node_modules/`、`**/dist/`（前端）
- `.env`、`.env.local`（密钥/敏感配置）
- Docker 数据卷目录（`pg_data/`、`qdrant_data/` 等）
