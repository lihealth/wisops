# V1.5 GitHub 配置清单

> 目标：将 `V1.0` 作为基线，启用 `V1.5` 的版本化研发流程。

## 1) 已在本地完成

- 已创建分支：`develop`
- 已创建标签：`v1.0.0`
- 已新增仓库配置：
  - `.github/pull_request_template.md`
  - `.github/ISSUE_TEMPLATE/feature_request.md`
  - `.github/ISSUE_TEMPLATE/bug_report.md`
  - `.github/ISSUE_TEMPLATE/config.yml`
  - `.github/workflows/ci.yml`

## 2) 待推送到 GitHub（网络恢复后执行）

```powershell
cd c:\Users\lijian22\wisops
git push -u origin develop
git push origin v1.0.0
git push   # 推送本地其它未推送提交
```

## 3) GitHub 页面配置（手工 5 分钟）

### 3.1 创建里程碑

- 进入仓库 -> `Issues` -> `Milestones` -> `New milestone`
- 标题：`V1.5`
- 描述：V1.5 功能迭代（graph-ui / graph-api / data / ops）

### 3.2 创建 Label

建议最小集合：

- 类型：`feature` `bug` `docs` `chore`
- 优先级：`P0` `P1` `P2`
- 模块：`graph-ui` `graph-api` `data` `ops`
- 版本：`v1.5`

### 3.3 配置分支保护

仓库 -> `Settings` -> `Branches`：

- 对 `main` 添加规则：
  - Require a pull request before merging
  - Require approvals (>=1)
  - Require status checks to pass
  - Block force pushes
  - Block deletions
- 对 `develop` 添加同样规则（可不强制审批数量）

### 3.4 创建 V1.0 Release

- 仓库 -> `Releases` -> `Draft a new release`
- Tag 选择：`v1.0.0`
- Title：`WisOps V1.0`
- Notes：填写 V1.0 完成项（RAG/图谱 API/图谱 UI/数据导入/文档）

## 4) V1.5 开发约定

- 所有开发从 `develop` 拉分支：
  - `feature/v1.5-xxx`
  - `fix/v1.5-xxx`
- PR 合并目标：`develop`
- 通过 CI 后再合并
- V1.5 收口后：`develop -> main`，打标签 `v1.5.0`

